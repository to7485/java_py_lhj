DB 마무리
MySQL이라는 dbms 사용을 했다.
DBeaver -> 연결하고 사용하기 위한 도구

MySQL -> workbench
ㄴ 자바에 연결

DDL : 데이터베이스 객체를 컨트롤 하는 명령어
데이터베이스, 테이블, 뷰, 프로시저...
CREATE
ALTER
DROP
TRUNCATE
--------------------------------테이블 만들기부터----------------------------
DML : 테이블 내의 데이터를 컨트롤하는 명령어
SELECT
INSERT
DELETE
UPDATE

데이터베이스 모델링
현실세계의 것을 데이터베이스화 시키는 것
0. 요구사항 분석
1. 개념모델링
2. 논리모델링
3. 물리모델링

다양한 종류들의 함수
문자함수
숫자함수
날짜함수
집계함수
윈도우함수

JOIN PK와 FK로 연결된 테이블에서 내용을 조회할 때 사용

INNER JOIN

LEFT OUTER JOIN

RIGHT OUTER JOIN

CROSS JOIN

SELF JOIN

뷰
가상의 테이블
읽기만 할 수 있고 추가, 수정,삭제는 불가능함
원본이 바뀌면 같이 바뀐다.

복잡한 쿼리문을 숨길 때 사용

서브쿼리
SQL문 안에 SQL문을 넣는것

WHERE절에 값으로 들어가거나
FROM절에 테이블로 들어간다.

정렬
ORDER BY 컬럼명 오름차순 OR 내림차순;
별칭이나 컬럼의 순서 가지고도 정렬할 수 있다.
--------------------------------------------------------- 시험범위
CASE문
SELECT 할 때 컬럼 안에 들어있는 값에 따라 출력하는 값을 정하는 문법

CASE
    WHEN 조건식 THEN 실행문,
    WHEN 조건식 THEN 실행문,
    WHEN 조건식 THEN 실행문,
    ELSE
END;

프로시저
내부에서 SQL문을 실행할 수 있게 해주는 함수
CREATE PROCEDURE 프로시저명 (매개변수....)
BEGIN
	SQL
END

CALL 프로시저명(값1,값2)



애플 cpu를 인텔 -> m1, m2,m3,m4


JDK(Jva Devlopment kit)

JRE(Java Runtime Environment) 자바 실행 환경
자바 프로그램을 실행하는데 필요한 코드들을 제공

JVM(Java Virtual Machine)
자바 프로그램을 실행해주는 프로그램


eyunp








































